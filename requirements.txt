# Finance Data Cleaning Pipeline — Dependencies
# Install with: pip install -r requirements.txt

# Core data processing
pandas>=2.0.0
numpy>=1.24.0

# Database connectivity
sqlalchemy>=2.0.0

# SQL Server / Azure SQL support (comment out if only using demo mode)
pyodbc>=4.0.39

# Optional: faster Parquet export support (uncomment if needed)
# pyarrow>=12.0.0
