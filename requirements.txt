# Finance Data Cleaning Pipeline — Dependencies

# Core data processing
pandas>=2.0.0
numpy>=1.24.0

# ── Optional: LOCAL database mode only — NOT used by the Streamlit app ──────────
# pyodbc cannot build on Streamlit Cloud without a packages.txt providing
# `unixodbc-dev`; that failing build is what froze the live deploy at an old
# version. app.py / llm_nlq.py / run_manifest.py never import these. To use the
# DB connector locally, uncomment BOTH lines AND add a packages.txt with `unixodbc-dev`.
# sqlalchemy>=2.0.0
# pyodbc>=4.0.39

# Streamlit app
streamlit>=1.35.0
plotly>=5.20.0

# Anomaly detection (v2.2)
scikit-learn>=1.3.0
