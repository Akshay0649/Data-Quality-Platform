# Core AI
anthropic>=0.40.0
openai>=1.0.0

# Audio capture + transcription (Whisper API mode)
sounddevice>=0.4.6
soundfile>=0.12.0
numpy>=1.24.0

# Screen capture + OCR
Pillow>=10.0.0
pytesseract>=0.3.10

# PDF resume parsing
PyMuPDF>=1.23.0

# Optional: local Whisper (no internet needed — heavy, ~1 GB model download)
# faster-whisper>=1.0.0

# Optional: macOS screen-share invisibility
# pyobjc-framework-AppKit>=10.0; sys_platform == "darwin"

# Optional: Linux screenshot fallback
# pyautogui>=0.9.54
